name: Java CI with Gradle

on:
  push:
    branches: '**'
  pull_request:
    branches: '**'
  workflow_dispatch: # Allows you to also trigger this job manually via the GitHub UI

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v6

    - name: Make Gradle Wrapper Executable
      run: chmod +x gradlew

    - name: Build and Run Gradle Tests
      run: ./gradlew build

    - name: Upload Gradle Bundle Artifact
      uses: actions/upload-artifact@v6
      with:
        name: gradle-build-output
        path: build/
        retention-days: 1

  test-GFS:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Toronto'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test GFS
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/GFS.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: GFS-output
        path: output/
        retention-days: 1

  test-RAP:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Toronto'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test RAP
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/RAP.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: RAP-output
        path: output/
        retention-days: 1

  test-HRRR:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Toronto'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test HRRR
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/HRRR.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: HRRR-output
        path: output/
        retention-days: 1

  test-HRDPS:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Toronto'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test HRDPS
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/HRDPS.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: HRDPS-output
        path: output/
        retention-days: 1

  test-RDPS:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Toronto'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test RDPS
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/RDPS.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: RDPS-output
        path: output/
        retention-days: 1

  test-GDPS:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Toronto'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test GDPS
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/GDPS.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: GDPS-output
        path: output/
        retention-days: 1

  test-GEPS:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Winnipeg'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test GEPS
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/GEPS.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: GEPS-output
        path: output/
        retention-days: 1

  test-REPS:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Winnipeg'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test REPS
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/REPS.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: REPS-output
        path: output/
        retention-days: 1

  test-GDWPS:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Toronto'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test GDWPS
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/GDWPS.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: GDWPS-output
        path: output/
        retention-days: 1

  test-RDWPS:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Toronto'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test RDWPS
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/RDWPS.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: RDWPS-output
        path: output/
        retention-days: 1

  test-Lake-Erie:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Toronto'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test Lake-Erie
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/Lake-Erie.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: Lake-Erie-output
        path: output/
        retention-days: 1

  test-Lake-Huron-Michigan:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Toronto'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test Lake-Huron-Michigan
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/Lake-Huron-Michigan.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: Lake-Huron-Michigan-output
        path: output/
        retention-days: 1

  test-Lake-Ontario:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Toronto'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test Lake-Ontario
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/Lake-Ontario.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: Lake-Ontario-output
        path: output/
        retention-days: 1

  test-Lake-Superior:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Toronto'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test Lake-Superior
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/Lake-Superior.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: Lake-Superior-output
        path: output/
        retention-days: 1

  test-RDPA:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Vancouver'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test RDPA
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/RDPA.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: RDPA-output
        path: output/
        retention-days: 1

  test-HRDPA:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Vancouver'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test HRDPA
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/HRDPA.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: HRDPA-output
        path: output/
        retention-days: 1

  test-NAM11:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Toronto'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test NAM11
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/NAM11.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: NAM11-output
        path: output/
        retention-days: 1


  test-NAM12:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Toronto'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test NAM12
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/NAM12.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: NAM12-output
        path: output/
        retention-days: 1


  test-NAM32:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Toronto'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test NAM32
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/NAM32.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: NAM32-output
        path: output/
        retention-days: 1


  test-CanSIPS:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'America/Toronto'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test CanSIPS
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/CanSIPS.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: CanSIPS-output
        path: output/
        retention-days: 1


  test-Montreal-TroisRivieres:
    runs-on: ubuntu-latest
    needs: build
    env:
      TZ: 'Pacific/Midway'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Download Build Artifacts
      uses: actions/download-artifact@v7
      with:
        name: gradle-build-output
        path: build/

    - name: Test Montreal-TroisRivieres
      timeout-minutes: 10
      run: xvfb-run --auto-servernum ./gradlew run --args="$(tr '\n' ' ' < test/grid/Montreal-TroisRivieres.txt) debug=1"

    - name: Upload Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: Montreal-TroisRivieres-output
        path: output/
        retention-days: 1
